<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/px80"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/main_title_bg"
    android:orientation="horizontal">

    <RelativeLayout
        android:layout_width="@dimen/px90"
        android:layout_height="@dimen/px80"
        android:gravity="center"
        android:layout_weight="0" >

        <ImageView
            android:id="@+id/viewMore"
            android:layout_width="@dimen/px60"
            android:layout_height="@dimen/px60"
            app:srcCompat="@drawable/view_more_white" >
        </ImageView>


        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:paddingLeft="0dp"
            android:paddingRight="0dp"
            android:layout_width="match_parent"
            app:contentInsetStart="0dp"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary">

            <TextView
                android:id="@+id/toolbar_title"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="LOGO"
                android:textColor="@color/colorAccent"
                android:textSize="32sp"
                android:textStyle="bold"
                tools:ignore="HardcodedText" />

        </androidx.appcompat.widget.Toolbar>


    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/relateBtn"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:background="@drawable/main_title_bg"
        android:layout_weight="1"
        android:textColor="#7f7f7f"
        android:textSize="@dimen/px12"
        android:textStyle="" >
    </RelativeLayout>

    <!-- LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="@dimen/px80"
        android:gravity="center"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/btnSearch"
            android:layout_width="@dimen/px60"
            android:layout_height="@dimen/px60"
            app:srcCompat="@drawable/button_select_dialog" />
        <ImageView
            android:id="@+id/btnChoice"
            android:layout_width="@dimen/px60"
            android:layout_height="@dimen/px60"
            app:srcCompat="@drawable/button_choice_dialog" />
    </LinearLayout -->


</LinearLayout>